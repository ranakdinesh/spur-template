# ── Stage 1: Dependencies (node_modules baked in here) ────────────────────
FROM node:20-alpine AS deps

WORKDIR /app

# Copy only package files — maximises layer cache
COPY ui/studio/package.json ui/studio/package-lock.json* ./
RUN npm ci --frozen-lockfile

# ── Stage 2: Build ─────────────────────────────────────────────────────────
FROM node:20-alpine AS builder

WORKDIR /app

# Shared theme/component library
COPY ui/shared /shared

# App source + deps
COPY --from=deps /app/node_modules ./node_modules
COPY ui/studio .

ENV NEXT_TELEMETRY_DISABLED=1

ARG NEXT_PUBLIC_API_URL=http://localhost:8080
ARG NEXT_PUBLIC_THEME=spur
ARG SPUR_APP_NAME=Spur

ENV NEXT_PUBLIC_API_URL=$NEXT_PUBLIC_API_URL
ENV NEXT_PUBLIC_THEME=$NEXT_PUBLIC_THEME
ENV SPUR_APP_NAME=$SPUR_APP_NAME

RUN npm run build

# ── Stage 3: Minimal runtime (standalone Next.js) ─────────────────────────
FROM node:20-alpine AS runner

WORKDIR /app

ENV NODE_ENV=production
ENV NEXT_TELEMETRY_DISABLED=1

# next.config.ts must have: output: 'standalone'
COPY --from=builder /app/.next/standalone ./
COPY --from=builder /app/.next/static     ./.next/static
COPY --from=builder /app/public           ./public

EXPOSE 3000

HEALTHCHECK --interval=15s --timeout=3s --start-period=15s --retries=3 \
    CMD wget -qO- http://localhost:3000 || exit 1

CMD ["node", "server.js"]
